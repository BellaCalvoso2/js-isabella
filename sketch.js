function setup(){
    creatwCanvas (600,600);
}

function draw() {
      background ("black");
}

function draw() {
    stroke ("blue");
    FileList("red");

    // console.log(mouseIsPressed)
    
    if(mouseIsPressed){
        rect (mouseX, mouseY, 20, 35);
    }
}
